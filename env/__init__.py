"""Training environment adapters and :class:`~engine.GameSnapshot` JSON I/O.

``balatro_lite_gym`` must be on ``sys.path`` before importing submodules
(see ``tests/conftest.py`` or the Colab notebook path setup).
"""
